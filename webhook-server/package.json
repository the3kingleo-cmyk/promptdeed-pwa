{
  "name": "promptdeed-webhook-server",
  "version": "1.0.0",
  "description": "Webhook server that auto-fulfills Promptdeed orders and delivers the unlock code via email",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^6.9.13",
    "crypto": "^1.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
